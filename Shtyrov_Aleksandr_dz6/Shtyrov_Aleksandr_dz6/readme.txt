Добрый день. Надеюсь, везде правильно понял, что от
меня хотели в задании, по тому что
после 1го все как-то подозрительно просто получается.
Первое раза 4 переделывал. Только в конце понял,
что проще всего параллельно пройтись по 2м элементам
вложенной последовательности. Еще пробовал сделать генератор
бесконечный, выдающий поочередно цыета. Отдельно сделал, но реализовать
его в теле класса не получилось. Отсюда вопрос:
можно ли создать в классе метод, состоящий из нескольких функций? Чтоб в
одной из этих функций self не использовался. Просто она была
вспомогательой и работала с данными основной функции (метода) и находилась
внутри тела класса?
Спасибо за проверку.


Проверка
"метод, состоящий из нескольких функций" - вообще это плохая идея, но бывают разные кейсы
дз 1 не ООП
дз 2 - верно
" def init(self, name, surname, wage, bonus, position=None):
super().init(name, surname, wage, bonus, position)" - см урок
дз 5 верно, кроме " def init(self, title):
super(Handle, self).init(title)"